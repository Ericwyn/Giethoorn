package pointoffer;

/**
 *
 * java 位运算
 *
 *      << 左移动，用 0 补位
 *      >> 右移动，正数用 0 补位，负数用 1 补位
 *
 *      >>> 无符号左移动，正负数都用 0 补位
 *
 *      & 位与，全为 1 时候为 1，否则为 0
 *      | 位或，全为 0 时候为 0，否则为 1
 *      ^ 位亦或，当一个为 1 一个为 0 时候，才会 1，否则为 0 ， 101 ^ 011 = 110
 *      ~ 位非，反过来，为 1 时候结果为 0 ，为 0 时候结果为 1
 *
 *      于此对应的 a >>= 1 ，代表 a = a >> 1，与 a += 1同理
 *
 * Created by Ericwyn on 18-5-15.
 */
public class BitCalculate {



}
